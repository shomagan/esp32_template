driver/i2c.h to driver/i2c_master.h
python script
regs description
    ind was removed
    type moved
    descriptions spread between files

guid how to form

figure out what to do with  link message functions vs queue functions

telegram change index to guid.

regs_description_list_add_new should init it immediatly

space_number unite with table_ind

what do we have

    first call always with main self registers

    then could be calls from main os but with aux registers like battery state if exist

    then could be calls from user task with additional registers

    max size ~15 of tables

    ind removed

    we can save table by table if it is littlefs based , then table should have a name and size
    saved address should show address for own table only(starts from 0 for each table)
